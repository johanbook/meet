# Copy to `.env` and adjust for your environment. Values are inlined at
# build/start time (EXPO_PUBLIC_*). The API base URL must NOT include a path -
# the generated client paths already start with "/api".
EXPO_PUBLIC_API_BASE_URL=https://app.meetly.site
EXPO_PUBLIC_AUTH_BASE_URL=https://app.meetly.site/auth/api